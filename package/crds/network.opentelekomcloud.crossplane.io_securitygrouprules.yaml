---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: securitygrouprules.network.opentelekomcloud.crossplane.io
spec:
  group: network.opentelekomcloud.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - opentelekomcloud
    kind: SecurityGroupRule
    listKind: SecurityGroupRuleList
    plural: securitygrouprules
    singular: securitygrouprule
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .spec.forProvider.direction
      name: DIRECTION
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: A SecurityGroupRule is a managed resource that represents an
          OpenTelekomCloud VPC Security Group Rule.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: A SecurityGroupRuleSpec defines the desired state of a SecurityGroupRule.
            properties:
              forProvider:
                description: |-
                  SecurityGroupRuleParameters are the configurable fields of a SecurityGroupRule.
                  All fields are immutable after creation — the OTC API does not support updates.
                properties:
                  action:
                    description: Action is the security group rule action.
                    enum:
                    - allow
                    - deny
                    type: string
                  description:
                    description: Description provides supplementary information about
                      the security group rule.
                    maxLength: 255
                    type: string
                  direction:
                    description: Direction is the traffic direction of the security
                      group rule.
                    enum:
                    - ingress
                    - egress
                    type: string
                  etherType:
                    description: EtherType is the IP version. Defaults to IPv4 when
                      omitted.
                    enum:
                    - IPv4
                    - IPv6
                    type: string
                  multiPort:
                    description: |-
                      MultiPort is the port or port range.
                      Can be a single port (80), a port range (1-30), or comma-separated ports (22,3389,80).
                    type: string
                  priority:
                    description: Priority is the rule priority. The value is from
                      1 to 100, where 1 is the highest priority.
                    maximum: 100
                    minimum: 1
                    type: integer
                  protocol:
                    description: |-
                      Protocol is the protocol type.
                      The value can be icmp, tcp, udp, icmpv6, or an IP protocol number.
                    type: string
                  remoteGroupId:
                    description: |-
                      RemoteGroupID is the ID of the remote security group.
                      Mutually exclusive with remoteIpPrefix.
                    type: string
                  remoteGroupIdRef:
                    description: RemoteGroupIDRef is a namespaced reference to a SecurityGroup
                      for remoteGroupId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  remoteGroupIdSelector:
                    description: RemoteGroupIDSelector selects a namespaced reference
                      to a SecurityGroup for remoteGroupId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  remoteIpPrefix:
                    description: |-
                      RemoteIPPrefix is the remote IP address or CIDR block.
                      Mutually exclusive with remoteGroupId.
                    type: string
                  securityGroupId:
                    description: SecurityGroupID is the ID of the security group to
                      which this rule belongs.
                    type: string
                  securityGroupIdRef:
                    description: SecurityGroupIDRef is a namespaced reference to a
                      SecurityGroup.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  securityGroupIdSelector:
                    description: SecurityGroupIDSelector selects a namespaced reference
                      to a SecurityGroup.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                required:
                - direction
                type: object
                x-kubernetes-validations:
                - message: one of securityGroupId, securityGroupIdRef or securityGroupIdSelector
                    is required
                  rule: has(self.securityGroupId) || has(self.securityGroupIdRef)
                    || has(self.securityGroupIdSelector)
                - message: direction is immutable after creation
                  rule: self.direction == oldSelf.direction
                - message: securityGroupId is immutable after creation
                  rule: '!has(oldSelf.securityGroupId) || self.securityGroupId ==
                    oldSelf.securityGroupId'
                - message: description is immutable after creation
                  rule: '!has(oldSelf.description) || self.description == oldSelf.description'
                - message: etherType is immutable after creation
                  rule: '!has(oldSelf.etherType) || self.etherType == oldSelf.etherType'
                - message: protocol is immutable after creation
                  rule: '!has(oldSelf.protocol) || self.protocol == oldSelf.protocol'
                - message: multiPort is immutable after creation
                  rule: '!has(oldSelf.multiPort) || self.multiPort == oldSelf.multiPort'
                - message: remoteGroupId is immutable after creation
                  rule: '!has(oldSelf.remoteGroupId) || self.remoteGroupId == oldSelf.remoteGroupId'
                - message: remoteIpPrefix is immutable after creation
                  rule: '!has(oldSelf.remoteIpPrefix) || self.remoteIpPrefix == oldSelf.remoteIpPrefix'
                - message: action is immutable after creation
                  rule: '!has(oldSelf.action) || self.action == oldSelf.action'
                - message: priority is immutable after creation
                  rule: '!has(oldSelf.priority) || self.priority == oldSelf.priority'
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            description: A SecurityGroupRuleStatus represents the observed state of
              a SecurityGroupRule.
            properties:
              atProvider:
                description: SecurityGroupRuleObservation are the observable fields
                  of a SecurityGroupRule.
                properties:
                  action:
                    description: Action is the observed rule action.
                    type: string
                  description:
                    description: Description is the observed description.
                    type: string
                  direction:
                    description: Direction is the observed traffic direction.
                    type: string
                  etherType:
                    description: EtherType is the observed IP version.
                    type: string
                  id:
                    description: ID is the OTC security group rule ID.
                    type: string
                  multiPort:
                    description: MultiPort is the observed port or port range.
                    type: string
                  priority:
                    description: Priority is the observed rule priority.
                    type: integer
                  projectId:
                    description: ProjectID is the project to which the rule belongs.
                    type: string
                  protocol:
                    description: Protocol is the observed protocol type.
                    type: string
                  remoteAddressGroupId:
                    description: RemoteAddressGroupID is the observed remote address
                      group ID.
                    type: string
                  remoteGroupId:
                    description: RemoteGroupID is the observed remote security group
                      ID.
                    type: string
                  remoteIpPrefix:
                    description: RemoteIPPrefix is the observed remote IP address
                      or CIDR block.
                    type: string
                  securityGroupId:
                    description: SecurityGroupID is the observed security group ID.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
