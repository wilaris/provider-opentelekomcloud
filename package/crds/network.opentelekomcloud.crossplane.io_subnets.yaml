---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: subnets.network.opentelekomcloud.crossplane.io
spec:
  group: network.opentelekomcloud.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - opentelekomcloud
    kind: Subnet
    listKind: SubnetList
    plural: subnets
    singular: subnet
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .spec.forProvider.cidr
      name: CIDR
      type: string
    - jsonPath: .status.atProvider.status
      name: STATUS
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: A Subnet is a managed resource that represents an OpenTelekomCloud
          VPC subnet.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: A SubnetSpec defines the desired state of a Subnet.
            properties:
              forProvider:
                description: SubnetParameters are the configurable fields of a Subnet.
                properties:
                  availabilityZone:
                    description: AvailabilityZone is the availability zone to which
                      the subnet belongs.
                    type: string
                  cidr:
                    description: CIDR is the subnet CIDR block.
                    maxLength: 18
                    minLength: 1
                    type: string
                  description:
                    description: Description is the subnet description.
                    maxLength: 255
                    type: string
                  dhcpEnable:
                    description: |-
                      DHCPEnable indicates whether DHCP is enabled for the subnet.
                      Defaults to true when omitted.
                    type: boolean
                  dnsList:
                    description: DNSList is the subnet DNS server list.
                    items:
                      type: string
                    type: array
                  gatewayIp:
                    description: GatewayIP is the subnet gateway IPv4 address.
                    maxLength: 15
                    minLength: 7
                    type: string
                  ipv6Enable:
                    description: IPv6Enable indicates whether IPv6 is enabled for
                      the subnet.
                    type: boolean
                  name:
                    description: Name is the subnet name.
                    maxLength: 64
                    minLength: 1
                    type: string
                  ntpAddresses:
                    description: NTPAddresses is the subnet NTP server address configuration.
                    type: string
                  primaryDns:
                    description: PrimaryDNS is the primary DNS server IPv4 address.
                    type: string
                  secondaryDns:
                    description: SecondaryDNS is the secondary DNS server IPv4 address.
                    type: string
                  tags:
                    additionalProperties:
                      type: string
                    description: Tags are subnet tags.
                    maxProperties: 20
                    type: object
                  vpcId:
                    description: VPCID is the VPC ID to which the subnet belongs.
                    type: string
                  vpcIdRef:
                    description: VPCIDRef is a namespaced reference to a VPC.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  vpcIdSelector:
                    description: VPCIDSelector selects a namespaced reference to a
                      VPC.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                required:
                - cidr
                - gatewayIp
                - name
                type: object
                x-kubernetes-validations:
                - message: one of vpcId, vpcIdRef or vpcIdSelector is required
                  rule: has(self.vpcId) || has(self.vpcIdRef) || has(self.vpcIdSelector)
                - message: cidr is immutable after creation
                  rule: self.cidr == oldSelf.cidr
                - message: gatewayIp is immutable after creation
                  rule: self.gatewayIp == oldSelf.gatewayIp
                - message: ipv6Enable is immutable after creation
                  rule: '!has(oldSelf.ipv6Enable) || self.ipv6Enable == oldSelf.ipv6Enable'
                - message: availabilityZone is immutable after creation
                  rule: '!has(oldSelf.availabilityZone) || self.availabilityZone ==
                    oldSelf.availabilityZone'
                - message: vpcId is immutable after creation
                  rule: '!has(oldSelf.vpcId) || self.vpcId == oldSelf.vpcId'
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            description: A SubnetStatus represents the observed state of a Subnet.
            properties:
              atProvider:
                description: SubnetObservation are the observable fields of a Subnet.
                properties:
                  availabilityZone:
                    description: AvailabilityZone is the observed availability zone.
                    type: string
                  cidr:
                    description: CIDR is the observed subnet CIDR.
                    type: string
                  cidrIpv6:
                    description: CIDRIPv6 is the observed IPv6 CIDR.
                    type: string
                  description:
                    description: Description is the observed subnet description.
                    type: string
                  dhcpEnable:
                    description: DHCPEnable is the observed subnet DHCP state.
                    type: boolean
                  dnsList:
                    description: DNSList is the observed subnet DNS list.
                    items:
                      type: string
                    type: array
                  gatewayIp:
                    description: GatewayIP is the observed subnet gateway.
                    type: string
                  gatewayIpv6:
                    description: GatewayIPv6 is the observed IPv6 gateway.
                    type: string
                  id:
                    description: ID is the OTC subnet ID.
                    type: string
                  ipv6Enable:
                    description: IPv6Enable is the observed subnet IPv6 state.
                    type: boolean
                  name:
                    description: Name is the observed subnet name.
                    type: string
                  networkId:
                    description: NetworkID is the observed OpenStack network ID.
                    type: string
                  ntpAddresses:
                    description: NTPAddresses is the observed NTP server address configuration.
                    type: string
                  primaryDns:
                    description: PrimaryDNS is the observed primary DNS server.
                    type: string
                  secondaryDns:
                    description: SecondaryDNS is the observed secondary DNS server.
                    type: string
                  status:
                    description: Status is the observed subnet lifecycle status.
                    type: string
                  subnetId:
                    description: SubnetID is the observed OpenStack subnet ID.
                    type: string
                  tags:
                    additionalProperties:
                      type: string
                    description: Tags are observed subnet tags.
                    type: object
                  vpcId:
                    description: VPCID is the observed VPC ID.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
